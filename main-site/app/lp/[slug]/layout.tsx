import type { ReactNode } from "react";

type LandingProxyLayoutProps = {
  children: ReactNode;
};

export default function LandingProxyLayout({ children }: LandingProxyLayoutProps) {
  return children;
}
